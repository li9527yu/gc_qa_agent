# 价格查询接口保留策略分析

> 分析时间: 2026-04-09  
> 问题: 是否只保留Agent对话接口就足够？

---

## 一、现有接口功能对比

### 1.1 接口功能矩阵

| 功能特性 | Agent对话 `/agent/chat` | 标准查询 `/query/price` | 对话式 `/query/dialogue` | 直接查询 `/query/price/direct` |
|---------|------------------------|------------------------|------------------------|------------------------------|
| **多轮对话** | ✅ 智能自动 | ✅ 支持(session) | ❌ 已废弃 | ❌ 单次 |
| **流式响应** | ❌ 非流式 | ✅ 流式 | ❌ 已废弃 | ✅ 流式 |
| **智能补全** | ✅ 自动提示 | ❌ 需前端处理 | ❌ 已废弃 | ❌ 需完整数据 |
| **结构化输入** | ❌ 仅文本 | ❌ 仅文本 | ❌ 已废弃 | ✅ 支持 |
| **性能** | 中等(多工具调用) | 快(直接查询) | ❌ 已废弃 | 快 |
| **灵活性** | 高(Agent决策) | 中(固定流程) | ❌ 已废弃 | 低(纯数据查询) |
| **调试便利** | 中 | 高 | ❌ 已废弃 | 高 |

### 1.2 各接口适用场景

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           用户使用场景分析                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  场景1: 自然语言对话查询 (80%用户)                                           │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 用户: "查一下钢筋价格"                                                │   │
│  │ 系统: "请问是哪个省份？"                                              │   │
│  │ 用户: "广东省深圳市"                                                  │   │
│  │ 系统: 返回价格分析结果                                                │   │
│  │                                                                      │   │
│  │ ✅ Agent接口完美支持                                                  │   │
│  │ ❌ 对话式接口已废弃                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  场景2: 已知完整条件，快速查询 (15%用户)                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 用户: "从信息价查广东省深圳市HRB400钢筋价格"                            │   │
│  │ 系统: 直接返回结果                                                    │   │
│  │                                                                      │   │
│  │ ✅ Agent接口支持，但有多余的候选搜索步骤                                │   │
│  │ ✅ 标准查询接口更高效（流式）                                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  场景3: 系统集成/结构化数据查询 (5%用户)                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 前端已有材料列表，用户点击查询                                          │   │
│  │ 输入: {materialName, province, city, ...} 结构化数据                  │   │
│  │                                                                      │   │
│  │ ❌ Agent接口不支持结构化输入                                          │   │
│  │ ✅ 直接查询接口专门支持                                                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、只保留Agent接口的可行性分析

### 2.1 ✅ Agent能完全替代的场景

| 场景 | 说明 |
|-----|------|
| 自然语言多轮对话 | Agent的核心能力，完全覆盖 |
| 实体补全引导 | Agent自动处理，比原对话式更智能 |
| 候选材料推荐 | Agent内部调用工具实现 |
| 价格分析与展示 | Agent返回完整分析结果 |

### 2.2 ⚠️ Agent无法完全覆盖的场景

| 场景 | 问题 | 解决方案 |
|-----|------|---------|
| **结构化数据输入** | Agent只接受文本message | 需要新增结构化输入支持 |
| **流式响应** | Agent当前非流式 | 需要新增流式Agent接口 |
| **工具直接调用** | 前端可能需要直接调用工具 | 保留 `/mcp/tools/call` |
| **调试与测试** | Agent黑盒，不便调试 | 保留工具接口便于测试 |

### 2.3 性能对比

```
标准查询流程耗时:
├─ 标准查询 /query/price:        ~2-3s (流式，首字200ms)
├─ Agent查询 /agent/chat:        ~3-5s (非流式，等待完整结果)
├─ 对话式 /query/dialogue:       ~1s (仅状态更新)
└─ 直接查询 /price/direct:       ~2-3s (流式)

Agent额外开销:
├─ 候选材料搜索: +200-500ms
├─ 多工具串行调用: +500ms-1s
└─ 响应组装: +100ms
```

---

## 三、推荐的接口保留策略

### 3.1 建议保留的接口

#### 必须保留（核心功能）

```python
# 1. Agent对话接口 - 主要面向用户的智能交互
POST /api/v1/agent/chat
POST /api/v1/agent/chat/stream    # 建议新增流式版本

# 2. MCP工具接口 - 供前端直接调用工具（调试+高级场景）
GET  /api/v1/mcp/tools            # 工具列表
POST /api/v1/mcp/tools/call       # 工具调用
```

#### 建议保留（特定场景）

```python
# 3. 标准价格查询 - 高效流式查询（性能敏感场景）
POST /api/v1/query/price

# 4. 直接查询 - 结构化数据输入（系统集成场景）
POST /api/v1/query/price/direct
```

#### 已废弃

```python
# 对话式查询接口 - 已停止服务，返回 HTTP 410
POST /api/v1/query/dialogue
POST /api/v1/query/dialogue/execute
GET  /api/v1/query/dialogue/{session_id}
DELETE /api/v1/query/dialogue/{session_id}
```

### 3.2 架构演进路线

```
Phase 1: 过渡期（已完成）
├─ /query/dialogue/* 接口已返回 HTTP 410 停止服务
├─ Agent接口作为主推荐
└─ 保留 Agent + 标准查询 + 直接查询

Phase 2: 稳定期（当前）
├─ 保留 Agent + MCP工具 + 标准查询 + 直接查询
├─ 标准查询和直接查询逐步通过Agent包装
└─ 完全统一的Agent交互模式
```

---

## 四、Agent接口增强建议

### 4.1 新增流式Agent接口

```python
# 当前Agent接口返回完整JSON，建议新增流式版本
POST /api/v1/agent/chat/stream

# 响应格式（SSE）:
event: thought
data: {"step": 1, "thought": "提取实体中..."}

event: tool_call
data: {"tool": "extract_entities", "params": {...}}

event: tool_result
data: {"tool": "extract_entities", "result": {...}}

event: message
data: {"chunk": "收到，您想查询..."}

event: final
data: {"complete_response": {...}}
```

### 4.2 Agent支持结构化输入

```python
# 扩展现有Agent接口支持结构化输入
POST /api/v1/agent/chat

{
  "message": "查一下这个价格",           # 用户消息（可选）
  "session_id": "abc123",
  "structured_input": {                 # 结构化输入（可选）
    "materialName": "钢筋",
    "province": "广东省",
    "city": "深圳市",
    "channel": "information_price"
  },
  "options": {
    "skip_candidate_search": true,      // 跳过候选搜索
    "direct_query": false               // 直接查询，不询问
  }
}
```

---

## 五、最终建议

### 5.1 短期策略（当前阶段）

**不建议只保留Agent接口**，原因：
1. 前端可能已有基于旧接口的实现
2. Agent接口当前非流式，用户体验不如流式接口
3. 缺少结构化输入支持

**推荐做法**：
```
对外提供:
├─ Agent接口 (/agent/chat) - 主推，用于新功能
├─ 标准查询 (/query/price) - 保留，向后兼容
├─ 直接查询 (/price/direct) - 保留，系统集成
└─ MCP工具 (/mcp/*) - 保留，供高级使用

已废弃:
└─ 对话式查询 (/query/dialogue/*) - 已返回 HTTP 410，请使用 /agent/chat
```

### 5.2 中期策略（优化后）

**可以只保留Agent + MCP工具**，前提是：
1. ✅ Agent接口支持流式响应
2. ✅ Agent接口支持结构化输入
3. ✅ 前端完全迁移到Agent模式

```
最终架构:
├─ Agent接口 - 统一入口
│   ├─ 自然语言对话
│   ├─ 结构化数据查询
│   └─ 流式/非流式可选
└─ MCP工具接口 - 供调试和扩展
```

### 5.3 实施建议

```python
# 1. 新增流式Agent接口（高优先级）
@app.post("/api/v1/agent/chat/stream")
async def agent_chat_stream(request: AgentQueryRequest):
    # 实现SSE流式响应
    pass

# 2. 扩展现有Agent支持结构化输入（中优先级）
class AgentQueryRequest(BaseModel):
    message: Optional[str] = None
    structured_input: Optional[PriceQueryEntities] = None
    # ...

# 3. 接口已停止服务（已完成）
@router.post("/api/v1/query/dialogue")
async def dialogue_query(...):
    raise HTTPException(410, "该接口已废弃，请使用 POST /api/v1/agent/chat")
```

---

## 六、总结

| 问题 | 答案 |
|-----|------|
| 只保留Agent是否足够？ | **当前不够**，增强后可以 |
| 需要哪些增强？ | 流式响应、结构化输入 |
| 当前推荐策略 | Agent + 标准查询 + 直接查询 |
| 长期目标 | 统一为Agent接口 |
| 何时可以移除其他接口？ | 前端完全迁移 + Agent增强完成后 |

---

**建议行动**:
1. `/query/dialogue/*` 接口已停止服务（返回 HTTP 410）
2. 前端全面迁移到 `/agent/chat` 或 `/agent/chat/stream`
3. 扩展Agent支持结构化输入
4. 推动前端逐步迁移到Agent接口
5. 3个月后评估是否可以移除标准查询接口
